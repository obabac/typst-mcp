[project]
name = "typst-mcp"
version = "0.1.0"
description = "MCP server for interacting with Typst, providing tools for LaTeX conversion, syntax validation, and image generation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]==1.8.0",
    "numpy==2.3.2",
    "pillow==11.3.0",
]
